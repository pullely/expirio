# cloudflare-r2

The private R2 bucket per environment (`expirio-documents-stage`,
`expirio-documents-prod`) that holds certificate scans and PDFs attached to
tracked expiry items. Bound by name as `DOCUMENTS` in `apps/expiry-worker`.
